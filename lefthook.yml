pre-commit:
    parallel: false
    commands:
        format:
            run: npm run fmt
            stage_fixed: true
        lint:
            run: npm run lint -- --fix
            stage_fixed: true
        typecheck:
            run: npm run typecheck
